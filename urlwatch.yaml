reporters:
  email:
    enabled: true
    method: smtp
    host: smtp.gmail.com
    port: 587
    tls: true
    user: "${URLWATCH_EMAIL_USER}"
    password: "${URLWATCH_EMAIL_PASS}"
    from: "watcher@example.com"
    to:   "あなた@example.com"
