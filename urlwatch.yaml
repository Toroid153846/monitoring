# report:
#   email:
#     enabled: true
#     keyring: false
#     subject: "urlwatch – {count} changes detected"
#     from:    "toroid153846@gmail.com"
#     to:      "toroid153846@gmail.com"

#     smtp:
#       host:     smtp.gmail.com
#       port:     587
#       starttls: true
#       user:     "${URLWATCH_EMAIL_USER}"
#       password: "${URLWATCH_EMAIL_PASS}"
report:
  email:
    enabled: true
    method: smtp
    from:    "${secrets.MAIL_USER}"
    to:      "${secrets.MAIL_USER}"

    smtp:
      host:     smtp.gmail.com
      auth:     true
      port:     587
      starttls: true
      user:     "${secrets.MAIL_USER}"
      insecure_password: "${secrets.MAIL_PASS}"
