# report:
#   email:
#     enabled: true
#     keyring: false
#     subject: "urlwatch – {count} changes detected"
#     from:    "toroid153846@gmail.com"
#     to:      "toroid153846@gmail.com"

#     smtp:
#       host:     smtp.gmail.com
#       port:     587
#       starttls: true
#       user:     "${URLWATCH_EMAIL_USER}"
#       password: "${URLWATCH_EMAIL_PASS}"
report:
  email:
    enabled: true
    method: smtp
    from:    "${MAIL_USER}"
    to:      "${MAIL_USER}"

    smtp:
      host:     smtp.gmail.com
      auth:     true
      port:     587
      starttls: true
      user:     "${MAIL_USER}"
      insecure_password: "${MAIL_PASS}"
